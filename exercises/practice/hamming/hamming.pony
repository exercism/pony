primitive HammingDistance
  fun apply(dna0: String, dna1: String): USize ? =>
    """
    Returns number of differences between two strands of DNA, known as
    the Hamming Distance.
    """

    /* As this is a partial function which can return an error we
       must provide a way that this stub function can raise an error.

       It will never execute of course                               */

    if false then error end

    0
