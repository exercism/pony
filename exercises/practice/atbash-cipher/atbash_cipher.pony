primitive Atbash
  fun encode(plaintext: String): String =>
    """
    Encodes given plaintext into corresponding ciphertext
    """

    ""

  fun decode(plaintext: String): String =>
    """
    Decodes given ciphertext into corresponding plaintext
    """

    ""
